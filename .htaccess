<IfModule mod_rewrite.c>
    RewriteEngine On

   
    RewriteBase /

    
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirige todo a la carpeta public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>